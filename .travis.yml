language: c
os:
    - linux
    - osx

env:
  global:
    - DOTPATH=~/.dotfiles
    - GOPATH=~

install:
  - curl -L https://raw.githubusercontent.com/orihuzak/dotfiles/master/etc/install | bash
  - cd $DOTPATH
  - make init

script:
  make --silent test
